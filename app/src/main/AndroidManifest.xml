<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="team.projects.madquizapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MADQuizApplication">
        <activity android:name=".Quiz.OldScoreActivity"></activity>
        <activity android:name=".ProfileActivity" />
        <activity android:name=".Quiz.ScoreActivity" />
        <activity android:name=".Quiz.QuizActivity" />
        <activity android:name=".Admin.ViewQuestionDetailsActivity" />
        <activity android:name=".Admin.ActivityAddQuestions" />
        <activity android:name=".Admin.ViewQuestionsActivity" />
        <activity android:name=".Admin.AddNewQuizActivity" />
        <activity android:name=".Quiz.QuizDetailsActivity" />
        <activity android:name=".SearchQuizActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".Admin.ManageQuizActivity" />
        <activity android:name=".ViewAllQuizActivity" />
        <activity android:name=".NewUserActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>